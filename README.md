# Workflow Toolkit

独立的 workflow 工具链仓库。

## 目录结构

- `.workflow/`：任务模板与任务数据
- `scripts/workflow/`：命令行脚本

## 快速开始

```bash
bash scripts/workflow/workflow.sh init
bash scripts/workflow/workflow.sh new "示例任务"
bash scripts/workflow/workflow.sh list
```
